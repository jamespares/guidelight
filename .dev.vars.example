# Local secrets for Guidelight (copy to .dev.vars — never commit)
# Stripe (required for card checkout / invoices)
# STRIPE_SECRET_KEY=sk_live_...   # or sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# Publishable key + portal login URL live in wrangler.jsonc vars (safe to commit)
