# Local secrets for Guidelight (copy to .dev.vars — never commit)
# Stripe (required for card checkout / invoices)
# WARNING: never commit live secrets. Use test keys for local development.
# STRIPE_SECRET_KEY=sk_test_...   # or sk_live_... in production only
# STRIPE_WEBHOOK_SECRET=whsec_...
# Publishable key + portal login URL should be set via wrangler secrets or env-specific vars.
